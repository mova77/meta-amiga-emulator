# The repository is public. The tooling and process configuration that drive it are not,
# and must never be committed here.
.claude/
.claude-flow/
.packs.yaml
CLAUDE.md
GEMINI.md
AGENTS.md

# Build
build/
out/
cmake-build-*/
compile_commands.json
.cache/

# Byte-code left behind by the CI scripts under .github/scripts/ when they are run or
# imported locally.
__pycache__/
*.pyc

# Media and ROMs — never, under any circumstance (ADR-ROM-03 D1)
*.adf
*.adz
*.dms
*.hdf
*.ipf
*.scp
*.rom
*.key
roms/
media/

# Editors and platforms
.DS_Store
.idea/
.vscode/
*.swp
